using UnityEngine;

public class _PlayerSettings : MonoBehaviour
{
    //this should contain a huge list of simple settings (playerprefs)
    /*
     * 
    Camera Controller
        public float sensitivity = 10f;
        public float scrollSensitivity = 10f;
        public bool useMouseCameraMovement = false;

    Mouse Manager
        private const float clickVsDragTime = .15f;
        private const float hoverTimer = .5f;

    */
}
